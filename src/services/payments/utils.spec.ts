import { expect } from 'chai';
import { getMonthRange } from './utils';

describe.only('unit test example => unit tests to be added beside its file', () => {
  it('true equal true', async () => {
    await getMonthRange();
    expect(true).to.equal(true);
  });
});
