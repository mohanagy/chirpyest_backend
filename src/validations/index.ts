import * as auth from './auth';
import * as users from './users';
import * as brands from './brands';

export const authValidation = auth;
export const usersValidation = users;
export const brandsValidation = brands;
